/*
#include "ITEM.h"
bool ITEM::SetUp(ITEM itemo)
{
//	m_Data = itemo;
//	if(FAILED(m_ImgIllu.Load(szIllu))) return false;
//	if(FAILED(m_ImgDot.Load(szDot))) return false;
	
	return true;
}
*/

// 아 괜히 옮겼어. commit하지 않으면 되겠지라.